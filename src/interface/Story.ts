export type IStory = {
    title: string,
    author: string,
    image: string,
    description: string,
}

export type ICategory = {
    title: string,
    description: string,
    active: boolean,
}